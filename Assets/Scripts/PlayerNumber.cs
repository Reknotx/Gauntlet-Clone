using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * <summary>Enum that defines the player number in order
 * to separate their messages.</summary>
 * 
 */
public static class PlayerNumber
{
    public enum PlayerNum
    {
        PlayerOne,
        PlayerTwo,
        PlayerThree,
        PlayerFour
    }

}
